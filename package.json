{
  "name": "domiguel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portfólio Dom Dot. Developments — funções serverless (Vercel) + Supabase",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "jsonwebtoken": "^9.0.2"
  }
}
