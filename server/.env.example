# Porta do servidor
PORT=4000

# Segredo para assinar os tokens JWT (troca por um valor aleatório em produção)
JWT_SECRET=troca-este-segredo-por-algo-aleatorio

# Credenciais do admin criadas no primeiro arranque (seed)
ADMIN_EMAIL=admin@domiguel.dev
ADMIN_PASSWORD=domiguel123

# Origem permitida para CORS (o frontend)
CLIENT_ORIGIN=http://localhost:5173
